#Alejandro Alvarez
#5/15/2023
#The program asks user for 2 numbers and the possibility to choose computing the sum or product of the number

#The user's input for two numbers
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

#The menu options
print("Select an operation:")
print("1. Compute the sum")
print("2. Compute the product")

#The user's choice
choice = int(input("select option (1 or 2): "))

#Compute the sum if choice is 1, compute the product if choice is 2
if choice == 1:
    result = num1 + num2
    operation = "sum"
elif choice == 2:
    result = num1 * num2
    operation = "product"
else:
    print("Invalid choice.")
    exit()

#The result
print("The", operation, "of", num1, "and", num2, "is:", result)
